const state={running:false,start:0,snapshots:[],nodes:[],lastLocal:null};
const byId=(id)=>document.getElementById(id);
const val=(id)=>byId(id)?.value||'';
function connectionBody(){const type=val('conn-type');return type==='tcp'?{type:'tcp',host:val('tcp-host')||null,tcp_port:Number(val('tcp-port'))||null}:{type:'serial',port:val('serial-port')||null};}
function setStatus(id,msg,mode='warning'){const el=byId(id); if(!el)return; el.textContent=msg; el.className=`status-box status-${mode}`;}
function lockButtons(lock){['test-connection','read-local','read-discovered','run-backup'].forEach(id=>{const b=byId(id); if(b)b.disabled=lock;}); state.running=lock;}
function updateConnVisibility(){if(val('conn-type')==='tcp'){byId('serial-group')?.classList.add('hidden');byId('tcp-host-group')?.classList.remove('hidden');byId('tcp-port-group')?.classList.remove('hidden');}else{byId('serial-group')?.classList.remove('hidden');byId('tcp-host-group')?.classList.add('hidden');byId('tcp-port-group')?.classList.add('hidden');}}
async function fetchJson(url,opts){const r=await fetch(url,opts);const j=await r.json();if(!r.ok||j.ok===false) throw new Error(j.error||j.detail||`request failed (${r.status})`);return j;}
async function runAction(label,fn){if(state.running)return;lockButtons(true);state.start=Date.now();setStatus('backup-status',`${label}...`);byId('op-state')&&(byId('op-state').textContent=label);try{const result=await fn();const sec=((Date.now()-state.start)/1000).toFixed(1);setStatus('backup-status',`${label} complete (${sec}s)`,'success');return result;}catch(e){setStatus('backup-status',`${label} failed: ${e.message}`,'error');throw e;}finally{lockButtons(false);}}
function renderLocalSummary(){const el=byId('local-node-summary'); if(!el) return; const s=state.lastLocal||{}; if(!Object.keys(s).length){el.innerHTML='<div class="muted">No local node read yet.</div>'; return;} el.innerHTML=`<div><strong>${s.source_node_label||s.source_node_long_name||'unknown'}</strong> (${s.source_node_short_name||'-'})</div><div class='muted'>id:${s.source_node_id||'-'} · fw:${s.firmware||s.version||'-'} · role:${s.role||'-'} · pos:${s.position||'-'}</div><div class='muted'>Last read: ${new Date().toISOString()}</div><details><summary>View details</summary><pre>${JSON.stringify(s,null,2)}</pre></details>`;}
function snapLabel(s){return `#${s.id} - ${s.source_node_short_name||'-'} / ${s.local_node_name||'unknown'} - ${s.connection_target||'-'} - ${s.status} - ${s.node_count} nodes - ${s.verified?'verified':'unverified'}`}
function renderSnapshots(){const el=byId('snap-state'); if(!el)return;el.innerHTML=state.snapshots.map(s=>`<div class='node'><div class='node-top'><strong>${snapLabel(s)}</strong><span class='muted'>${s.created_at||''}</span></div><div class='actions'><a class='button-link' href='/snapshots/${s.id}'>Review</a><button data-del='${s.id}'>Delete</button></div></div>`).join('')||"<div class='empty'>No snapshots yet.</div>";}
async function refresh(){const snaps=await fetchJson('/api/snapshots');state.snapshots=snaps.items||[];renderSnapshots();}
async function loadReviewPage(){if(!window.SNAPSHOT_ID)return;const root=byId('review-root');const d=await fetchJson(`/api/snapshots/${window.SNAPSHOT_ID}`);const n=d.normalized||{};const sections={local_info:!!n.local_node,node_list:Array.isArray(d.nodes)&&d.nodes.length>0,channels:!!n.channels,config:!!n.config,module_config:!!n.module_config,errors:(n.errors||[]).length>0||(n.warnings||[]).length>0};root.innerHTML=`<div class='list'><div class='node'>snapshot id: ${d.snapshot.id}</div><div class='node'>source node id: ${d.snapshot.local_node_id||'-'}</div><div class='node'>source long name: ${d.snapshot.local_node_name||'-'}</div><div class='node'>source short name: ${d.snapshot.source_node_short_name||'-'}</div><div class='node'>target: ${d.snapshot.connection_target||'-'}</div><div class='node'>status: ${d.snapshot.status}</div><div class='node'>node count: ${d.snapshot.node_count}</div><div class='node'>local info: ${sections.local_info?'captured':'No local info captured'}</div><div class='node'>node list: ${sections.node_list?'captured':'No node list captured'}</div><div class='node'>channels: ${sections.channels?'captured':'No channel data captured'}</div><div class='node'>config: ${sections.config?'captured':'No config data captured'}</div><div class='node'>module config: ${sections.module_config?'captured':'No module config captured'}</div><div class='node'>errors/warnings: ${sections.errors?'present':'none'}</div><div class='actions'><button id='verify-snap'>Verify snapshot</button><button id='reject-snap'>Reject snapshot</button></div></div>`;
byId('verify-snap')?.addEventListener('click',async()=>{if(!confirm('Confirm verification?'))return;await fetchJson(`/api/snapshots/${window.SNAPSHOT_ID}/verify`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({note:'verified from review page'})});location.href='/';});
byId('reject-snap')?.addEventListener('click',async()=>{if(!confirm('Reject snapshot?'))return;await fetchJson(`/api/snapshots/${window.SNAPSHOT_ID}/reject`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({note:'rejected from review page'})});location.href='/';});}
byId('conn-type')?.addEventListener('change',updateConnVisibility);
byId('test-connection')?.addEventListener('click',()=>runAction('Testing connection',async()=>{const r=await fetchJson('/api/connections/test',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(connectionBody())});setStatus('connection-status',r.ok?'Connection ok':'Connection failed',r.ok?'success':'error');}));
byId('read-local')?.addEventListener('click',()=>runAction('Reading local node',async()=>{const r=await fetchJson('/api/nodes/read-local',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(connectionBody())});state.lastLocal=r.source_node||{};renderLocalSummary();}));
byId('read-discovered')?.addEventListener('click',()=>runAction('Reading discovered nodes',async()=>{await fetchJson('/api/nodes/read-discovered',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(connectionBody())});await refresh();}));
byId('run-backup')?.addEventListener('click',()=>runAction('Running full backup',async()=>{await fetchJson('/api/backups/local',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(connectionBody())});await refresh();}));
byId('snap-state')?.addEventListener('click',async(e)=>{const d=e.target.closest('button[data-del]'); if(!d)return; const id=d.getAttribute('data-del'); if(!confirm(`Delete snapshot #${id}?`)) return; await fetchJson('/api/snapshots/cleanup',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({mode:'single',snapshot_id:Number(id),confirm:true})}); await refresh();});
byId('cleanup-unverified')?.addEventListener('click',async()=>{if(!confirm('Delete all unverified snapshots?')) return; await fetchJson('/api/snapshots/cleanup',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({mode:'unverified',confirm:true})}); await refresh();});
byId('cleanup-empty')?.addEventListener('click',async()=>{if(!confirm('Delete failed/empty snapshots?')) return; await fetchJson('/api/snapshots/cleanup',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({mode:'failed_or_empty',confirm:true})}); await refresh();});
updateConnVisibility();refresh();loadReviewPage();
